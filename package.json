{
  "name": "geom-scraper",
  "version": "0.1.0",
  "private": true,
  "description": "Geometry-based web scraper that extracts product cards without relying on CSS classes or XPath",
  "type": "module",
  "scripts": {
    "build": "tsc -p .",
    "scrape": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "playwright": "^1.49.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
